<h1>Liste des auteurs</h1>
<table border="1" cellpadding="15" cellspacing="5">

            <tr>
                <th>ID</th>
                <th>Username</th>
                <th>email</th>
                <th>Action</th>
            </tr>

            {% for i in list %}
                <tr>
                    <td>{{ i.id }}</td>
                    <td>{{ i.Username }}</td>
                    <td>{{ i.email }}</td>
                    <td><a href="{{ path('delectAuthors', {'id': i.id}) }}">delete</a></td>
                    <td><a href="{{ path('delectAuthors', {'id': i.id}) }}">Show</a></td>
                    <td><a href="{{ path('delectAuthors', {'id': i.id}) }}">delete</a></td>

                </tr>

                

            {% endfor %}
</table>